import 'package:flutter/material.dart';

class StyleStep extends StatelessWidget {
  const StyleStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Step 2: Choose Style (Placeholder)'));
  }
}
