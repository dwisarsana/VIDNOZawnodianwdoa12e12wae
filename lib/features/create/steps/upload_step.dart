import 'package:flutter/material.dart';

class UploadStep extends StatelessWidget {
  const UploadStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Step 1: Upload Photos (Placeholder)'));
  }
}
